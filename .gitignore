# Build outputs
build/
.gradle/
node_modules/
dist/
.next/

# Lighthouse 자동 측정 결과 — scripts/lighthouse-mobile.sh 산출물 (HTML + JSON)
lighthouse-reports/

# Pipeline (팀 공유 — Compaction 방어 + 협업 목적)
# state, artifacts, decisions 모두 커밋하여 팀원 간 파이프라인 컨텍스트 공유

# Environment
.env
.env.*
!.env.example
!.env.prod.example
# 프로덕션 시크릿 — 절대 커밋 금지 (위 .env.* 규칙으로 이미 제외되지만 명시)
.env.prod
.env.prod.local

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Claude Code local overrides (커밋된 settings.json 의 개인 커스터마이징용)
.claude/settings.local.json
